# 📊 SysMetrics - Руководство по Логированию

**Date:** 2025-12-10  
**Version:** 2.0  
**Status:** Production Ready  

---

## 🎯 Обзор

SysMetrics использует **структурированное логирование** с тегами для отладки работы программы и отслеживания данных, отображаемых на экране Android TV.

### Основные теги логирования

| Тег | Назначение | Пример использования |
|-----|-----------|---------------------|
| `OVERLAY_SERVICE` | Lifecycle сервиса overlay | Создание/уничтожение сервиса, инициализация |
| `OVERLAY_UPDATE` | Цикл обновления метрик | Начало/конец цикла, производительность |
| `OVERLAY_DISPLAY` | Что показывается на экране | Актуальные значения на дисплее ATV |
| `OVERLAY_SETTINGS` | Изменения настроек | Загрузка конфигурации |
| `METRICS_CPU` | Расчёт загрузки CPU | Delta, baseline, проценты |
| `METRICS_RAM` | Использование памяти | Свободная/занятая память |
| `METRICS_BASELINE` | Инициализация baseline | Первые измерения CPU |
| `METRICS_ERROR` | Ошибки в MetricsCollector | Исключения при чтении /proc |
| `PROC_TOP` | Топ приложений | Сбор и сортировка приложений |
| `PROC_CPU` | CPU отдельных процессов | Per-process CPU расчёты |
| `PROC_RAM` | RAM отдельных процессов | Per-process память |
| `PROC_NAME` | Имена приложений | PackageManager resolution |
| `PROC_ERROR` | Ошибки ProcessStatsCollector | Исключения при работе с процессами |

---

## 🔍 Как Отслеживать Работу Программы

### 1. Базовый мониторинг (все логи SysMetrics)

```bash
adb logcat | grep -E "OVERLAY_|METRICS_|PROC_"
```

**Что вы увидите:**
- ✅ Запуск/остановка сервиса
- 📊 Метрики в реальном времени
- 📺 Что реально показывается на экране
- ⚠️ Предупреждения и ошибки

### 2. Только что отображается на экране

```bash
adb logcat -s OVERLAY_DISPLAY:D
```

**Пример вывода:**
```
OVERLAY_DISPLAY: 📺 CPU on SCREEN: 'CPU: 45%' color=GREEN
OVERLAY_DISPLAY: 📺 RAM on SCREEN: 'RAM: 1234/2048 MB' (60.3%)
OVERLAY_DISPLAY: 📺 SELF on SCREEN: 'Self: 1.5% / 42M'
OVERLAY_DISPLAY: 📺   #1: YouTube: 23% / 567MB
OVERLAY_DISPLAY: 📺   #2: Chrome: 12% / 234MB
OVERLAY_DISPLAY: 📺   #3: Spotify: 8% / 156MB
```

### 3. Отладка проблем с CPU (детальный лог)

```bash
adb logcat -s METRICS_CPU:V METRICS_BASELINE:D
```

**Пример вывода:**
```
METRICS_BASELINE: 🔧 Initializing CPU baseline...
METRICS_BASELINE: ✅ CPU baseline initialized
METRICS_BASELINE:    user=123456, nice=0, system=45678, idle=987654, iowait=1234, irq=0, softirq=567
METRICS_BASELINE:    total=1158589, active=169701 (14.64%)

METRICS_CPU: 📊 Current CPU stats: total=1159234, active=170012, idle=988012
METRICS_CPU: 📈 CPU: totalΔ=645, idleΔ=358, activeΔ=311 → 48.22% (active/total=48.22%)
METRICS_CPU: 🟡 MODERATE CPU: 48.2%
```

### 4. Отладка топ-приложений

```bash
adb logcat -s PROC_TOP:D PROC_CPU:V
```

**Пример вывода:**
```
PROC_TOP: 🔍 Getting top 3 apps (sortBy=combined)
PROC_TOP: 📱 Found 87 running processes
PROC_TOP: 📊 Collected 15 user apps with measurable usage
PROC_TOP: 🏆 #1: YouTube - CPU=23.4%, RAM=567MB, Score=238.7
PROC_TOP: 🏆 #2: Chrome - CPU=12.1%, RAM=234MB, Score=123.3
PROC_TOP: 🏆 #3: Spotify - CPU=8.7%, RAM=156MB, Score=88.6
```

### 5. Производительность циклов обновления

```bash
adb logcat -s OVERLAY_UPDATE:V
```

**Пример вывода:**
```
OVERLAY_UPDATE: 🔄 Update cycle #1670854025 started
OVERLAY_UPDATE: 📊 Metrics collected: CPU=45.2%, RAM=1234/2048MB (60.3%)
OVERLAY_UPDATE: ✅ Update cycle completed in 23ms
OVERLAY_UPDATE: ⚠️ Slow update cycle: 105ms (should be <100ms)  ← Предупреждение о медленном цикле
```

### 6. Lifecycle сервиса

```bash
adb logcat -s OVERLAY_SERVICE:I
```

**Пример вывода:**
```
OVERLAY_SERVICE: ✅ MinimalistOverlayService created
OVERLAY_SERVICE: 📦 Collectors initialized
OVERLAY_SERVICE: 🔧 Starting baseline initialization...
OVERLAY_SERVICE: 🎯 Step 1: First baseline measurement
OVERLAY_SERVICE: ✅ Baseline initialized - waiting for delta...
OVERLAY_SERVICE: 🎯 Step 2: Second measurement for delta
OVERLAY_SERVICE: ✅ Baseline ready - Initial CPU: 12.5% - starting metrics updates
OVERLAY_SERVICE: 🎨 Creating overlay view...
OVERLAY_SERVICE: 📋 View references: CPU=true, RAM=true, Self=true, Apps=true
OVERLAY_SERVICE: 📐 Window params: x=27, y=108, gravity=51
OVERLAY_SERVICE: ✅ Overlay view created and added to window
```

---

## 🐛 Диагностика Проблем

### Проблема: CPU показывает 0%

**Команда диагностики:**
```bash
adb logcat -s METRICS_BASELINE:* METRICS_CPU:D
```

**Что искать:**
- ✅ `CPU baseline initialized` - baseline создан
- ⏳ `First reading stored as baseline, returning 0%` - первое чтение (нормально)
- ⚠️ `Zero or negative totalΔ` - проблема с delta
- ⚠️ `Baseline not initialized` - не инициализирован baseline

**Решение:**
- Убедитесь, что прошло >1 секунды после запуска
- Проверьте доступ к `/proc/stat`: `adb shell cat /proc/stat`
- Увеличьте `BASELINE_INIT_DELAY` в коде

### Проблема: Топ-приложения не отображаются

**Команда диагностики:**
```bash
adb logcat -s PROC_TOP:D PROC_ERROR:E
```

**Что искать:**
- 📱 `Found X running processes` - сколько процессов найдено
- 📊 `Collected X user apps` - сколько пользовательских приложений
- ⚠️ Если `Collected 0 user apps` - все приложения системные

**Решение:**
- Запустите несколько приложений на ATV
- Проверьте `top_apps_count` в настройках
- Убедитесь, что приложения действительно используют ресурсы

### Проблема: Overlay не появляется на экране

**Команда диагностики:**
```bash
adb logcat -s OVERLAY_SERVICE:D
```

**Что искать:**
- ✅ `Overlay view created and added to window` - view создан
- ❌ `Failed to create overlay view` - ошибка создания
- 📋 `View references: CPU=false` - view не найден в layout

**Решение:**
- Проверьте разрешение `SYSTEM_ALERT_WINDOW`
- Убедитесь, что `overlay_minimalist.xml` существует
- Проверьте все `android:id` в layout

### Проблема: Медленная работа

**Команда диагностики:**
```bash
adb logcat -s OVERLAY_UPDATE:V
```

**Что искать:**
- ⚠️ `Slow update cycle: Xms` - цикл обновления медленный
- Норма: <50ms, Приемлемо: <100ms, Проблема: >100ms

**Решение:**
- Уменьшите `top_apps_count` (меньше приложений)
- Увеличьте `UPDATE_INTERVAL_MS` (реже обновления)
- Проверьте загрузку системы

---

## 📈 Уровни Логирования

SysMetrics использует стандартные уровни Android Log:

| Уровень | Когда используется | Пример |
|---------|-------------------|--------|
| `V` (Verbose) | Детальная информация для разработчика | Каждое чтение /proc/stat |
| `D` (Debug) | Отладочная информация | Расчёты CPU, сбор метрик |
| `I` (Info) | Важные события | Инициализация, lifecycle |
| `W` (Warning) | Предупреждения (не критично) | Медленный цикл, высокая нагрузка |
| `E` (Error) | Ошибки | Исключения, недоступные файлы |

### Фильтрация по уровню

**Только важное (Info и выше):**
```bash
adb logcat -s OVERLAY_SERVICE:I METRICS_CPU:I PROC_TOP:I
```

**Всё подробно (Debug и выше):**
```bash
adb logcat -s OVERLAY_SERVICE:D METRICS_CPU:D PROC_TOP:D
```

**Максимальная детализация (Verbose):**
```bash
adb logcat -s OVERLAY_SERVICE:V METRICS_CPU:V PROC_TOP:V
```

---

## 🎨 Эмодзи в Логах (для Быстрой Визуализации)

| Эмодзи | Значение |
|--------|----------|
| ✅ | Успешно выполнено |
| ❌ | Ошибка |
| ⚠️ | Предупреждение |
| 🔧 | Инициализация |
| 📊 | Данные/метрики |
| 📺 | Отображение на экране |
| 🔄 | Обновление/цикл |
| 🎯 | Важный этап |
| 🏆 | Топ приложение |
| 🟢 | Низкая нагрузка (<50%) |
| 🟡 | Средняя нагрузка (50-80%) |
| 🔴 | Высокая нагрузка (>80%) |
| 📱 | Приложение/процесс |
| 🔥 | Cache warm-up |
| ⏳ | Ожидание |
| 🧮 | Математический расчёт |

---

## 🚀 Быстрый Старт

### Стандартный мониторинг (рекомендуется)

```bash
# 1. Очистить логи
adb logcat -c

# 2. Запустить приложение на ATV
adb shell am start -n com.sysmetrics.app/.ui.MainActivity

# 3. Следить за работой
adb logcat | grep -E "(OVERLAY_DISPLAY|METRICS_CPU|PROC_TOP)" --line-buffered
```

### Сохранить логи в файл (для анализа)

```bash
# Записать 5 минут работы
adb logcat | grep -E "OVERLAY_|METRICS_|PROC_" > sysmetrics_logs.txt

# Ctrl+C через 5 минут
# Анализировать файл:
cat sysmetrics_logs.txt | grep "OVERLAY_DISPLAY"
```

### Мониторинг только ошибок

```bash
adb logcat -s METRICS_ERROR:E PROC_ERROR:E OVERLAY_SERVICE:E
```

---

## 📋 Checklist Отладки

Используйте этот чеклист для диагностики проблем:

- [ ] **Сервис запущен**: `OVERLAY_SERVICE: ✅ MinimalistOverlayService created`
- [ ] **Baseline инициализирован**: `METRICS_BASELINE: ✅ CPU baseline initialized`
- [ ] **View создан**: `OVERLAY_SERVICE: ✅ Overlay view created`
- [ ] **Метрики собираются**: `METRICS_CPU: 📈 CPU: totalΔ=...`
- [ ] **Данные на экране**: `OVERLAY_DISPLAY: 📺 CPU on SCREEN: ...`
- [ ] **Топ-приложения**: `PROC_TOP: 🏆 #1: ...`
- [ ] **Производительность OK**: `OVERLAY_UPDATE: ✅ Update cycle completed in <50ms`
- [ ] **Нет ошибок**: Проверить `*_ERROR:E` теги

---

## 🔬 Примеры Реальных Сценариев

### Сценарий 1: "CPU всегда 0%"

**Логи:**
```
METRICS_BASELINE: ✅ CPU baseline initialized
METRICS_BASELINE:    total=1000000, active=150000 (15.00%)
METRICS_CPU: ⚠️ CPU: totalΔ=0 (zero or negative delta!)
```

**Диагноз:** Слишком быстрое повторное чтение (delta=0)

**Решение:** Увеличить `UPDATE_INTERVAL_MS` или `BASELINE_INIT_DELAY`

### Сценарий 2: "Приложения не показываются"

**Логи:**
```
PROC_TOP: 📱 Found 87 running processes
PROC_TOP: 📊 Collected 0 user apps with measurable usage
```

**Диагноз:** Все процессы системные или использование <0.01%

**Решение:** Запустить YouTube/Chrome, подождать 2-3 секунды

### Сценарий 3: "Лаги/фризы"

**Логи:**
```
OVERLAY_UPDATE: ⚠️ Slow update cycle: 245ms (should be <100ms)
```

**Диагноз:** Обработка занимает слишком много времени

**Решение:** 
- Уменьшить `top_apps_count` (3 → 1)
- Увеличить `UPDATE_INTERVAL_MS` (500ms → 1000ms)

---

## 📞 Поддержка

Если проблема не решается:

1. **Соберите логи:**
   ```bash
   adb logcat -d > full_logs.txt
   ```

2. **Найдите все ошибки:**
   ```bash
   grep -E "(ERROR|❌)" full_logs.txt
   ```

3. **Проверьте system info:**
   ```bash
   adb shell cat /proc/cpuinfo
   adb shell cat /proc/meminfo
   adb shell getprop ro.build.version.release  # Android version
   ```

4. **Создайте issue на GitHub** с логами и system info

---

## ✅ Чек-лист для README

Следующая информация добавлена в README:

- [x] Таблица тегов логирования
- [x] Команды для мониторинга
- [x] Примеры вывода логов
- [x] Диагностика типовых проблем
- [x] Быстрый старт для отладки

---

**Составлено:** Senior Android Developer  
**Дата:** 2025-12-10  
**Версия:** 2.0 (Production)
