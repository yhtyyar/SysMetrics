<?xml version="1.0" encoding="utf-8"?>
<!-- Simplified Overlay Layout for TvOverlay_cpu Pattern -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_overlay"
    android:padding="16dp">

    <!-- Title -->
    <TextView
        android:id="@+id/overlay_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="14sp"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        android:layout_marginBottom="12dp" />

    <!-- CPU Metric -->
    <LinearLayout
        android:id="@+id/cpu_metric_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="⚙ CPU"
                android:textSize="12sp"
                android:textColor="@color/icon_cpu" />

            <TextView
                android:id="@+id/cpu_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="●"
                android:textSize="14sp"
                android:textColor="@color/metric_success"
                tools:textColor="@color/metric_error" />
        </LinearLayout>

        <TextView
            android:id="@+id/cpu_value"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="0.0%"
            android:textSize="24sp"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            tools:text="48.5%" />

        <ProgressBar
            android:id="@+id/cpu_progress"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="4dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/progress_bar_dynamic"
            tools:progress="48" />
    </LinearLayout>

    <!-- RAM Metric (Used / Total) -->
    <LinearLayout
        android:id="@+id/ram_metric_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="💾 RAM"
                android:textSize="12sp"
                android:textColor="@color/icon_ram" />

            <TextView
                android:id="@+id/ram_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="●"
                android:textSize="14sp"
                android:textColor="@color/metric_success"
                tools:textColor="@color/metric_warning" />
        </LinearLayout>

        <TextView
            android:id="@+id/ram_value"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="0 / 0 MB"
            android:textSize="18sp"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            android:letterSpacing="0.05"
            tools:text="1250 / 1699 MB" />

        <ProgressBar
            android:id="@+id/ram_progress"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="4dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/progress_bar_dynamic"
            tools:progress="73" />
    </LinearLayout>

    <!-- Temperature Metric -->
    <LinearLayout
        android:id="@+id/temp_metric_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🌡 Temp"
            android:textSize="12sp"
            android:textColor="@color/icon_temp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/temp_value"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/not_available"
            android:textSize="20sp"
            android:textColor="@color/metric_success"
            android:textStyle="bold"
            tools:text="45°C" />
    </LinearLayout>

    <!-- Footer Info -->
    <TextView
        android:id="@+id/cores_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/text_quaternary"
        tools:text="Cores: 4" />

</LinearLayout>
